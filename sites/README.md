# Sites

- Three React sites, the faces of the kernel.
- `git` is the code browser, a projection of this repo (git.mrly.net).
- `net` is the landing site (mrly.net).
- `web` is the face, riding the wasm kernel (web.mrly.net).
- The terminal face moved to `pkgs/rs/mrlycli`.
