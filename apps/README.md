# Apps

The faces of the kernel: a terminal CLI, a desktop GUI, the wasm web face, the React landing site (mrly.net), the React projection of this repo (git.mrly.net), and the retired React playground.
