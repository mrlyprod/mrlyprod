# Pkgs

The crates everything stands on: the mrlyrs kernel family, mrlypy for Python, and mrlyjs for wasm. Two are not crates at all: mrlycss holds the stylesheets the sites share, mrlygpu the WebGPU projector, both TypeScript packages.
