# Pkgs

- The packages everything stands on, one folder per language.
- `rs` is the Rust crates: the kernel family and the terminal face.
- `py` is the Python binding, built by maturin.
- `js` is the web packages: `mrlyui` the design system, `mrlygpu` the WebGPU projector, `web` the wasm binding.
