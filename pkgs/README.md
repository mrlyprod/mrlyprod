# Pkgs

The crates everything stands on: the mrlyrs kernel family, mrlypy for Python, and mrlyjs for wasm. Three are not crates at all: mrlyui is the design system and component library (with its kitchen sink demo) that every site rides, and mrlygpu is the WebGPU projector, all TypeScript packages.
