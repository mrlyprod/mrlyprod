# Pkgs

The crates everything stands on: the mrlyrs kernel family, mrlypy for Python, and mrlyjs for wasm. Three are not crates at all: mrlyui is the design system and component library (with its kitchen sink demo), mrlycss and mrlydom dress the current sites until they ride mrlyui, and mrlygpu is the WebGPU projector, all TypeScript packages.
