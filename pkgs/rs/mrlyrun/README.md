# mrlyrun

The runner: effects performed against the world.

A reserved claim, honestly held: today it paces retries - `backoff(attempt)` in milliseconds, doubling on the Mrly beat and resting at eight seconds.
