[package]
name = "mrlyfiles"
version.workspace = true
edition.workspace = true
description = "A shelf for up to 24 files, held as base64 and served back as data URIs."
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"

[dependencies]
mrlycore.workspace = true
mrlyos.workspace = true

[dev-dependencies]
mrlycore = { workspace = true, features = ["testkit"] }
mrlyos = { workspace = true, features = ["testkit"] }
