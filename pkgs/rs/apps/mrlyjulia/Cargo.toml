[package]
name = "mrlyjulia"
version.workspace = true
edition.workspace = true
description = "An endless seeded dive through Julia sets, hunting the edge of the set one cycle at a time."
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"

[dependencies]
mrlycore.workspace = true
mrlymath.workspace = true
mrlyos.workspace = true

[dev-dependencies]
mrlycore = { workspace = true, features = ["testkit"] }
mrlyos = { workspace = true, features = ["testkit"] }
