[package]
name = "mrlymandelbrot"
version.workspace = true
edition.workspace = true
description = "An endless seeded zoom into the Mandelbrot set: each cycle dives at a point on the edge, fades, and begins somewhere new."
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"

[dependencies]
mrlycore.workspace = true
mrlymath.workspace = true
mrlyos.workspace = true

[dev-dependencies]
mrlycore = { workspace = true, features = ["testkit"] }
mrlyos = { workspace = true, features = ["testkit"] }
