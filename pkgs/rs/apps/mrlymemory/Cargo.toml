[package]
name = "mrlymemory"
version.workspace = true
edition.workspace = true
description = "The card-matching game with a decoy: seeded deals, a brief look before the flip, and rounds that carry the score."
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"

[dependencies]
mrlycore.workspace = true
mrlyos.workspace = true
mrlyskin.workspace = true

[dev-dependencies]
mrlycore = { workspace = true, features = ["testkit"] }
mrlyos = { workspace = true, features = ["testkit"] }
