[package]
name = "mrlytennis"
version.workspace = true
edition.workspace = true
description = "Paddle and ball against a seeded wall of blocks that creeps a row closer every dozen or so returns."
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"

[dependencies]
mrlycore.workspace = true
mrlyos.workspace = true
mrlyskin.workspace = true

[dev-dependencies]
mrlycore = { workspace = true, features = ["testkit"] }
mrlyos = { workspace = true, features = ["testkit"] }
