[package]
name = "mrlyphotos"
version.workspace = true
edition.workspace = true
description = "A wall for the twelve most recent photos, oldest off the back, over a counter of every shot ever taken."
license.workspace = true
repository.workspace = true
homepage.workspace = true
readme = "README.md"

[dependencies]
mrlycore.workspace = true
mrlyos.workspace = true

[dev-dependencies]
mrlycore = { workspace = true, features = ["testkit"] }
mrlyos = { workspace = true, features = ["testkit"] }
