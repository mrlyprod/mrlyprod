# The diagonal cut

- 2026-08-28 [Conjecture] Cook's (2011) Menger-slice code cuts along the normal `(1, 1, 0.5)`, not the centroid diagonal, so the base-3 diagonal cut is not Cook's slice; the reference row scoring the cut as Cook's is kept at `yes` with that correction recorded beside it, and the row once scored `yes` in error now reads `no`.
