{
  "name": "mrly-demos",
  "private": true,
  "type": "module",
  "scripts": {
    "wasm": "bash ../scripts/wasm.sh",
    "dev": "bun index.html */index.html",
    "build": "rm -rf dist && bun build index.html */index.html --outdir dist --splitting --minify --chunk-naming 'lib-[hash].[ext]' --asset-naming '[name]-[hash].[ext]'",
    "check": "bun check.ts"
  },
  "dependencies": {
    "three": "^0.185.1"
  }
}
