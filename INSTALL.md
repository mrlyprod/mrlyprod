# Install

The apps in your terminal. One binary, no dependencies:

```sh
curl -fsSL https://mrly.net/install.sh | sh
```

Puts `mrly` in `~/.local/bin` and warns if that is not on your
PATH. macOS (arm64, x86_64) and Linux (x86_64, aarch64). Run
`mrly` to play.
